---
language: node_js

sudo: false

env:
  - NODE_ENVIRONMENT=test

cache:
  directories:
    - node_modules

before_install:
  - "npm config set spin false"
  - "npm install -g npm@^2"

install:
  - ./bin/install

script:
  - npm test
